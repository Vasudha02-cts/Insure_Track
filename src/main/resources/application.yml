spring:
  datasource:
    url: jdbc:mysql://localhost:3306/InsureTrack
    username: root
    password: Anusha@2645933
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      # Change 'update' to 'create' just for this run
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8084